import react from 'react'
import MaterialIcons from 'react-native-vector-icons/MaterialIcons'

const BackIconLfet = (props)=>{
    return (
        <MaterialIcons name='keyboard-arrow-left' color={props.color} size={props.size} />
    )
}
export default BackIconLfet